import * as types from './../constants/indexConstant';
const initState = []
export default (state = initState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

